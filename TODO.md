# TODO

## Fix React build for portfolio

- [ ] Rewrite `portfolio/src/App.jsx` into a valid React component (remove `<html>/<head>/<body>` and inline `<script>` tags).
- [ ] Remove/replace CDN/inline Three.js + Framer Motion code so file compiles under Vite.
- [ ] Run `npm run dev` and `npm run build` to verify no compile/runtime errors.

